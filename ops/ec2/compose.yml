services:
  api:
    build:
      context: ../../
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    env_file: /home/ec2-user/trainingapp-api/shared/api.env
    volumes:
      - /home/ec2-user/trainingapp-api/shared/data:/app/data
      - /home/ec2-user/trainingapp-api/shared/backups:/app/backups
    expose:
      - "4100"
    networks:
      stack_web:
        aliases:
          - trainingapp-api

networks:
  stack_web:
    external: true
